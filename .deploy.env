# .deploy.env —— 部署默认配置（仓库内）
# 端口与监听
PORT=8000
HOST=0.0.0.0

# 安装策略：no=就地更新；yes=备份并覆盖（备份在 /opt/huandan-backups/）
AUTO_CLEAN=no

# 分支与仓库地址（install_root.sh 会用到；在仓库内运行时不强制要求）
BRANCH=main
REPO=https://github.com/aidaddydog/huandan.server.git

# 数据目录（PDF/上传分离）
DATA=/opt/huandan-data

# 会话密钥（生产请更换）
SECRET_KEY=please-change-me

# BASE 默认自动识别为“当前仓库根目录”，通常不需要配置
# BASE=/opt/huandan-server
