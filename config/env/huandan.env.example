# 复制到 /etc/huandan.env 并按需修改
HUANDAN_BASE=/opt/huandan-server
HUANDAN_DATA=/opt/huandan-data
PORT=8000
HOST=0.0.0.0
PYBIN=python3

# 会话密钥（务必改成随机字符串；当前 main.py 仍使用内置值，可后续按需接入）
SESSION_SECRET=please-change-me-very-long-random

# 直出或反代：DIRECT(直出) / NGINX(走 80/443 反代)
EXPOSE_MODE=DIRECT
